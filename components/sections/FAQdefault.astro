---
---

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
  <!-- Header -->
  <div class="text-center mb-16">
    <h2 class="text-4xl md:text-5xl font-black text-gray-900 mb-4">
      Frequently Asked Questions
    </h2>
    <p class="text-lg md:text-xl text-gray-600 max-w-2xl mx-auto">
      Get answers to common questions about our services
    </p>
  </div>

  <!-- FAQ Grid - Two Independent Columns (Desktop only) -->
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    
    <!-- Left Column -->
    <div class="space-y-6">
      <div class="bg-white border border-gray-200 rounded-lg shadow-2xl overflow-hidden">
        <button 
          class="faq-toggle-left w-full p-6 flex items-start gap-4 hover:bg-gray-50 transition text-left" 
          data-faq="faq1"
        >
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-900">How fast can I see results?</h3>
          </div>
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2" 
            class="faq-icon w-5 h-5 text-gray-400 flex-shrink-0 mt-1 transition-transform duration-300"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-content hidden px-6 pb-6">
          <p class="text-gray-600">We can often start seeing results as fast as week 2. That gives us time to build the landing pages and ads properly.</p>
        </div>
      </div>

      <div class="bg-white border border-gray-200 rounded-lg shadow-2xl overflow-hidden">
        <button 
          class="faq-toggle-left w-full p-6 flex items-start gap-4 hover:bg-gray-50 transition text-left" 
          data-faq="faq3"
        >
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-900">What exactly do you build for me?</h3>
          </div>
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2" 
            class="faq-icon w-5 h-5 text-gray-400 flex-shrink-0 mt-1 transition-transform duration-300"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-content hidden px-6 pb-6">
          <p class="text-gray-600">A high-converting landing page built on our proprietary framework, ad creation, and ongoing management and optimization.</p>
        </div>
      </div>

      <div class="bg-white border border-gray-200 rounded-lg shadow-2xl overflow-hidden">
        <button 
          class="faq-toggle-left w-full p-6 flex items-start gap-4 hover:bg-gray-50 transition text-left" 
          data-faq="faq5"
        >
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-900">Do I get to keep the landing page?</h3>
          </div>
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2" 
            class="faq-icon w-5 h-5 text-gray-400 flex-shrink-0 mt-1 transition-transform duration-300"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-content hidden px-6 pb-6">
          <p class="text-gray-600">Landing pages are built on our proprietary framework and are not transferable. If you want lightning-fast performance on our platform, that stays with us. However, if you opt for a website on WordPress, Webflow, Wix, or Shopify, you can migrate it away (though you'll need a license for the builder we used).</p>
        </div>
      </div>

      <div class="bg-white border border-gray-200 rounded-lg shadow-2xl overflow-hidden">
        <button 
          class="faq-toggle-left w-full p-6 flex items-start gap-4 hover:bg-gray-50 transition text-left" 
          data-faq="faq7"
        >
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-900">What if I don't want your platform?</h3>
          </div>
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2" 
            class="faq-icon w-5 h-5 text-gray-400 flex-shrink-0 mt-1 transition-transform duration-300"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-content hidden px-6 pb-6">
          <p class="text-gray-600">We only build landing pages on our proprietary platform. However, for websites, we can build on WordPress, Webflow, Wix, or Shopify – and those can be migrated if you ever leave.</p>
        </div>
      </div>
    </div>

    <!-- Right Column -->
    <div class="space-y-6">
      <div class="bg-white border border-gray-200 rounded-lg shadow-2xl overflow-hidden">
        <button 
          class="faq-toggle-right w-full p-6 flex items-start gap-4 hover:bg-gray-50 transition text-left" 
          data-faq="faq2"
        >
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-900">Do you guarantee leads?</h3>
          </div>
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2" 
            class="faq-icon w-5 h-5 text-gray-400 flex-shrink-0 mt-1 transition-transform duration-300"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-content hidden px-6 pb-6">
          <p class="text-gray-600">No. Lead volume is often dictated by market demand, which we don't control. However, if there is demand in your market, our ads and landing pages have produced consistent results across many verticals.</p>
        </div>
      </div>

      <div class="bg-white border border-gray-200 rounded-lg shadow-2xl overflow-hidden">
        <button 
          class="faq-toggle-right w-full p-6 flex items-start gap-4 hover:bg-gray-50 transition text-left" 
          data-faq="faq4"
        >
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-900">Do I pay for ads separately?</h3>
          </div>
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2" 
            class="faq-icon w-5 h-5 text-gray-400 flex-shrink-0 mt-1 transition-transform duration-300"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-content hidden px-6 pb-6">
          <p class="text-gray-600">Yes. You add your card to either Meta or Google depending on the strategy. Ad budgets are separate from our service fees.</p>
        </div>
      </div>

      <div class="bg-white border border-gray-200 rounded-lg shadow-2xl overflow-hidden">
        <button 
          class="faq-toggle-right w-full p-6 flex items-start gap-4 hover:bg-gray-50 transition text-left" 
          data-faq="faq6"
        >
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-900">Can I use my existing ads account?</h3>
          </div>
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2" 
            class="faq-icon w-5 h-5 text-gray-400 flex-shrink-0 mt-1 transition-transform duration-300"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-content hidden px-6 pb-6">
          <p class="text-gray-600">No. Older ads accounts often come with baggage from previous agencies that can interfere with tracking. We set up fresh accounts to guarantee clean data and proper machine learning. We do love looking at your existing account to see if there's anything worth salvaging though.</p>
        </div>
      </div>

      <div class="bg-white border border-gray-200 rounded-lg shadow-2xl overflow-hidden">
        <button 
          class="faq-toggle-right w-full p-6 flex items-start gap-4 hover:bg-gray-50 transition text-left" 
          data-faq="faq8"
        >
          <div class="flex-1">
            <h3 class="text-lg font-bold text-gray-900">Why are your sites faster than WordPress or Wix?</h3>
          </div>
          <svg 
            xmlns="http://www.w3.org/2000/svg" 
            viewBox="0 0 24 24" 
            fill="none" 
            stroke="currentColor" 
            stroke-width="2" 
            class="faq-icon w-5 h-5 text-gray-400 flex-shrink-0 mt-1 transition-transform duration-300"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-content hidden px-6 pb-6">
          <p class="text-gray-600">Our sites are pre-rendered with no database, limited JavaScript, and no plugins. This makes them significantly faster than typical no-code, blogging, or DIY platforms. We design every site for a specific purpose: increasing conversions. Speed and performance directly impact your bottom line.</p>
        </div>
      </div>
    </div>

  </div>
</div>

<script is:inline>
  // Ensure all FAQs start hidden on page load
  window.addEventListener('DOMContentLoaded', function() {
    document.querySelectorAll('.faq-content').forEach(content => {
      content.classList.add('hidden');
    });
  });

  // LEFT COLUMN - Independent accordion
  document.querySelectorAll('.faq-toggle-left').forEach(button => {
    button.addEventListener('click', function(e) {
      e.preventDefault();
      const card = this.closest('.bg-white');
      const content = card.querySelector('.faq-content');
      const icon = this.querySelector('.faq-icon');
      
      // Close all OTHER FAQs in LEFT column only
      document.querySelectorAll('.faq-toggle-left').forEach(otherButton => {
        if (otherButton !== this) {
          const otherCard = otherButton.closest('.bg-white');
          const otherContent = otherCard.querySelector('.faq-content');
          const otherIcon = otherButton.querySelector('.faq-icon');
          otherContent.classList.add('hidden');
          otherIcon.classList.remove('rotate-180');
        }
      });
      
      // Toggle current FAQ
      content.classList.toggle('hidden');
      icon.classList.toggle('rotate-180');
    });
  });

  // RIGHT COLUMN - Independent accordion
  document.querySelectorAll('.faq-toggle-right').forEach(button => {
    button.addEventListener('click', function(e) {
      e.preventDefault();
      const card = this.closest('.bg-white');
      const content = card.querySelector('.faq-content');
      const icon = this.querySelector('.faq-icon');
      
      // Close all OTHER FAQs in RIGHT column only
      document.querySelectorAll('.faq-toggle-right').forEach(otherButton => {
        if (otherButton !== this) {
          const otherCard = otherButton.closest('.bg-white');
          const otherContent = otherCard.querySelector('.faq-content');
          const otherIcon = otherButton.querySelector('.faq-icon');
          otherContent.classList.add('hidden');
          otherIcon.classList.remove('rotate-180');
        }
      });
      
      // Toggle current FAQ
      content.classList.toggle('hidden');
      icon.classList.toggle('rotate-180');
    });
  });
</script>